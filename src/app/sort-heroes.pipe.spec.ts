import { SortHeroesPipe } from './sort-heroes.pipe';

describe('SortHeroesPipe', () => {
  it('create an instance', () => {
    const pipe = new SortHeroesPipe();
    expect(pipe).toBeTruthy();
  });
});
